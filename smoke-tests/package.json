{
  "name": "smoke-tests",
  "version": "1.0.0",
  "description": "Pruebas de humo para el Sistema de Gestión Académica",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:verbose": "node --test --test-reporter=tap tests/*.test.js"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
